on:
  issue_comment:
    types: [created, updated, deleted]

jobs:
  name: "Log reactions"
  steps:
  steps:
    - run: |
        echo A comment on PR $NUMBER
      env:
        REACTIONS: ${{ github.event.issue.reactions }}
